
( function( module ) {

	'use strict';

	var userService = function( authService ) {

		//authService.check()
	};

	if ( angular ) {

		angular.module( module )
		.service( 'userService', userService );

	}

} )( 'sysUsers' );
